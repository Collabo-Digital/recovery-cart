name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "https://recovery-cart.vercel.app/webhooks/app/uninstalled"

[commands]
predev = "npx prisma generate"
dev = "npm exec react-router dev"
